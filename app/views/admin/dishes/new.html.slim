fieldset
  legend Update menu here
  =form_for [:admin, @dish] do |f|  
    = f.select(:course, options_for_select(["", :First, :Second, :Drink], include_blank: true))    
    =f.label 'Name'
    =f.text_field :name    
    =f.label 'Price'
    =f.number_field :price              
    = f.submit